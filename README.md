wait this is published on github?

based on remorseism and judge and zcity

Container loot presentation is shared in `lua/homigrad/cl_container_loot_grid.lua`; sandbox props and `zbox_lootbox` keep separate server transfer handlers.
